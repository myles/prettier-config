import mylesPrettierConfig from "./index.js";

/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  ...mylesPrettierConfig,
};

export default config;
